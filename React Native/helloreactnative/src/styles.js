import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  mainContainer: {},
  text: {
    fontSize: 24,
    color: 'red',
    fontWeight: 'bold',
    borderWidth: 5,
    borderColor: 'green',
    textAlign: 'center',
  },
});

export {styles};
