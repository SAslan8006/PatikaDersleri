import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {

        margin: 10,
        flexDirection: "row",
        justifyContent: "space-between"
    },
    title: {
        fontSize: 40,
        fontWeight: "bold",
        color: "#EB98AD"
    },
    counter: {
        fontSize: 40,
        fontWeight: "bold",
        color: "#452D33"
    }
})