import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  input: {
    backgroundColor: '#ededed',
    margin: 5,
    marginBottom: 10,
    borderRadius: 10,
    padding: 10,
  },
});