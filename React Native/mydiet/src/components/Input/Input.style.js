import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        padding: 8,
        margin: 10,
        borderBottomWidth: 1,
        borderColor: "white"

    },
    input: {
        color: "white",
        fontSize: 20
    }
})