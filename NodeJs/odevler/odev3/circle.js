const circleArea = (r) => {
    return Math.PI * r ** 2;
  };
  
  const circleCircumFerence = (r) => {
    return 2 * Math.PI * r;
  };
  
  module.exports = {
    circleArea,
    circleCircumFerence,
  };